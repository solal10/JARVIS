"use client";

import React from 'react';
import BaseHero from './BaseHero';

export default function HeroContact() {
  return (
    <BaseHero
      title="Contactez-nous"
      subtitle="Un projet ? Une question ? Parlons-en !"
    />
  );
}
